























0 ->var
GetUnitPosition(<-var)
AFakeCommand(<-var)

# This warning can be disabled in the extension config
<-unassignedVariable

if
endif 

endif
if

"UnmatchedBracket" ())

"#Unclosed string